# API Keys — Set the key for your preferred LLM provider.
# OpenAI is the default provider.
OPENAI_API_KEY=...
ANTHROPIC_API_KEY=...
FIREWORKS_API_KEY=...
GROQ_API_KEY=...

# Ollama runs locally — no API key needed. Just ensure Ollama is running.
# Configure models in config.yaml instead.

# Model selection is now configured in config.yaml.
# See config.yaml for all available settings.
